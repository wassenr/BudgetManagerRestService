package service;

public class Test {

	/* Tests for Accounts mappings */
	/* --------------------------- */

	// account -- gets all accounts (GET)

	// account/{accoundid} -- gets account with id = accountid (GET)

	// account -- updates an Account (POST)

	// account -- insert a new Account (POST)

	// account/{accountid} -- delete an account (DELETE)

	/* Tests for Budgetss mappings */
	/* --------------------------- */

	// budget -- get all budgets (GET)

	// budget/event/{eventid} -- gets budgets for event with id = eventid (GET)

	// budget/{budgetid} -- gets the budget with id = budgetid (GET)

	// budget -- insert a new budget (POST)

	// budget -- update a budget (PUT)

	// budget/{budgetid} -- delete a budget (DELETE)

}
